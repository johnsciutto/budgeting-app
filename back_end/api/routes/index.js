const { userRouter } = require('./users');
const { categoryRouter } = require('./categories');
const { transactionRouter } = require('./transaction');

module.exports = { userRouter, categoryRouter, transactionRouter }
